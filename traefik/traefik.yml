api:
  dashboard: true

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https

  websecure:
    address: ":443"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    watch: true
    network: traefik-public

certificatesResolvers:
  letsencrypt:
    acme:
      email: paulandresgalloerazo@gmail.com
      storage: /letsencrypt/acme.json
      httpChallenge:
        entryPoint: web